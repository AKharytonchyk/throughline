"""Throughline — a local, single-user tool that shows where a developer's Claude Code
context budget goes, and finds recurring tool-call chains worth collapsing into one
intent-based tool.

100% local. No network. Read-only toward Claude Code. See .specify/memory/constitution.md.
"""

__version__ = "0.1.0"
